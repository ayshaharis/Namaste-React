const Grocery=()=>{
    return(
        <div>
                  <h1>Lazy Loading|Dynamic Bundling|Chunking concepts </h1>
                  <p>This session is created to learn concept of dynamic bundling. How to optimize code and increase perfomance as thge app grows bigger in size. As it grows bigger its always ideal to split code and do chunking. Susupense component is used for that. here Grocery file is lazy loaded using lazy(). so this code wont appear in the main indesx.js . it only gets int o the browser whenver the user demands</p>

        </div>
    
    );
};
export default Grocery;
